version: '3.8'

networks:
  net:

volumes:
  pgdata:
    driver: local
    driver_opts:
      o: bind
      type: none
      device: /var/lib/psql/data
  assets:
    driver: local
    driver_opts:
      o: bind
      type: none
      device: /opt/vireo/data

services:

  db:
    image: postgres
    environment:
       - POSTGRES_DB=vireo4
       - POSTGRES_USER=vireo
       - POSTGRES_PASSWORD=vireo
    volumes:
      - pgdata:/var/lib/pgsql/data
    ports:
      - 5432:5432
    networks:
      - net

  adminer:
    image: adminer
    ports:
      - 8080:8080
    depends_on:
      - db
    networks:
      - net

  vireo:
    container_name: vireo
    hostname: vireo
    image: ${IMAGE_HOST}/${SERVICE_PROJECT}/${SERVICE_PATH}:${IMAGE_VERSION}
    build:
      dockerfile: Dockerfile
      context: './'
    env_file:
       - .env
    volumes:
      - assets:${APP_PATH}
    ports:
      - 9000:9000
    depends_on:
      - db
    networks:
      - net
