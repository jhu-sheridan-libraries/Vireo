[Unit]
Description=Vireo ETD Management System
After=network.target

[Service]
Type=exec
User=vireo
Group=vireo
WorkingDirectory=/opt/vireo/Vireo
ExecStart=/usr/bin/mvn clean spring-boot:run
ExecStartPost=/bin/sh -c 'echo $MAINPID > /opt/vireo/vireo.pid'
ExecStop=/bin/kill -TERM $MAINPID
ExecStopPost=/bin/rm -f /opt/vireo/vireo.pid
PIDFile=/opt/vireo/vireo.pid
Restart=on-failure
RestartSec=10
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target